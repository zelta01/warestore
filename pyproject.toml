[project]
name = "steam-cache-login-tool"
dynamic = ["version"]
description = "Steam cache login and account management tool"
requires-python = ">=3.12"
dependencies = [
    "chardet>=5.0",
    "cryptography>=50.0",
    "gevent>=26.0",
    "gevent-eventemitter>=2.1",
    "pillow>=12.3.0",
    "protobuf>=6.33.5,<7",
    "psutil>=5.9",
    "pyjwt>=2.8",
    "pyqt5>=5.15,<6",
    "pywin32>=305",
    "requests>=2.28,<3",
    # ValvePython's unmaintained `client` extra incorrectly pins protobuf <4.
    # Declare those client dependencies directly so patched protobuf can be used.
    "steam>=1.4.4",
    "vdf>=3.4",
]

[project.scripts]
warestore = "warestore.presentation.account_manager.main:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/warestore"]

[tool.hatch.version]
path = "src/warestore/__init__.py"

[tool.uv]
package = true
required-environments = ["sys_platform == 'win32'"]

[dependency-groups]
dev = ["pyinstaller", "pytest>=8.0"]

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["src", "."]
